<table class="list categories">
	<tr>
		<th><%= l(:field_identifier) %></th>
		<th><%= l(:field_name) %></th>
	</tr>
	<% @categories.each do |category| %>
		<tr>
			<td><%= category.id %></td>
			<td><%= category.name %></td>
		</tr>
	<% end %>
</table>